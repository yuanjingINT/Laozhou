{
  "name": "publish_skill",
  "display_name": "发布技能",
  "description": "校验并原子发布 Skill 草稿。创建草稿绝不覆盖；更新草稿执行版本检查；scripts 不会自动注册为工具。",
  "parameters": {
    "type": "object",
    "properties": {
      "draft_id": {
        "type": "string",
        "description": "create_skill 或 update_skill 返回的草稿 ID。"
      }
    },
    "required": ["draft_id"],
    "additionalProperties": false
  },
  "permission": "writes",
  "always_loaded": false,
  "load_policy": "group",
  "groups": ["skills"]
}
