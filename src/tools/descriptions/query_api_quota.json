{
  "name": "query_api_quota",
  "display_name": "查询大模型 API 额度",
  "description": "Query and summarize the configured DeepSeek or OpenRouter API quota.",
  "parameters": {
    "type": "object",
    "properties": {
      "provider": {
        "type": "string",
        "enum": ["all", "deepseek", "openrouter"],
        "description": "Provider to query. Defaults to all configured providers."
      },
      "account": {
        "type": "string",
        "description": "Optional account name. Omit to query every configured account."
      }
    },
    "additionalProperties": false
  },
  "permission": "readonly",
  "always_loaded": false,
  "load_policy": "summary",
  "groups": ["utility"]
}
