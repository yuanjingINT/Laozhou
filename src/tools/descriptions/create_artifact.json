{
  "name": "create_artifact",
  "display_name": "创建预览文件",
  "description": "在 Miyu 托管的 Artifact 工作区创建或更新完整交付物，并立即展示到 WebUI。用户明确要求报告、文档、网页、表格、数据文件、独立代码文件或可下载成品时优先调用。内容完成后再调用；普通项目源码修改不要调用。",
  "parameters": {
    "type": "object",
    "properties": {
      "filename": {
        "type": "string",
        "description": "包含正确扩展名的安全文件名，例如 report.md、index.html 或 data.json；不允许包含目录。"
      },
      "content": {
        "type": "string",
        "description": "完整的最终文件内容。"
      },
      "title": {
        "type": "string",
        "description": "可选显示标题。"
      }
    },
    "required": ["filename", "content"],
    "additionalProperties": false
  },
  "always_loaded": true,
  "load_policy": "summary",
  "groups": ["dev"]
}
